class Article {
  final String id;
  final String title;
  final String summary;
  final String content;
  final DateTime created;

  Article(this.id, this.title, this.summary, this.content, this.created);
}